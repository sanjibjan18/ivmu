= form_for [movie, Comment.new] do |f|
  = render '/shared/error_display', {:error_object => f.object}

  .field
    = f.label :title
    = f.text_field :title
  .field
    = f.label :comment
    = f.text_area :comment 
 
  .actions
    = f.submit 'Save'
