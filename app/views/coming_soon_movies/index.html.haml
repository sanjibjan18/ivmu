= render 'shared/meta_title_and_keywords', { :key => 'home'}
= content_for :heading do
  Comming soon

.containerIn
  = render :partial => 'shared/movies', :locals => { :from => 'coming_soon'}
  .clear
  .right
    = will_paginate @movies, {:page_links => false}
  .clear

