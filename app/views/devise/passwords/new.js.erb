$('#registration').dialog('destroy');
$('#registration').html('<%= escape_javascript(render "new") %>');
$('#registration').dialog({ modal: true,  width: 600, height: 350, title: 'Muvi.in Forgot password' });

